# Полная инструкция по настройке доступа к репозиторию на новом компьютере

## 📋 Подготовка

### Шаг 1: Клонирование репозитория (если еще не клонирован)

```bash
# Перейдите в нужную директорию
cd ~/projects  # или любая другая директория

# Клонируйте репозиторий
git clone https://github.com/letoceiling-coder/avangard.git

# Перейдите в директорию проекта
cd avangard
```

### Шаг 2: Проверка текущей конфигурации

```bash
# Проверьте remote URL
git remote -v

# Должно показать:
# origin  https://github.com/letoceiling-coder/avangard.git (fetch)
# origin  https://github.com/letoceiling-coder/avangard.git (push)
```

---

## 🔑 Вариант 1: Настройка через Personal Access Token (рекомендуется для быстрого старта)

### Шаг 1: Создание токена в GitHub

1. Откройте браузер и перейдите: https://github.com/settings/tokens
2. Нажмите **"Generate new token"** → **"Generate new token (classic)"**
3. Заполните форму:
   - **Note**: `Avangard Deploy` (или любое другое название)
   - **Expiration**: выберите срок действия (например, 90 дней или No expiration)
   - **Select scopes**: отметьте **`repo`** (полный доступ к репозиториям)
4. Нажмите **"Generate token"** внизу страницы
5. **⚠️ ВАЖНО: Скопируйте токен сразу!** Он показывается только один раз и выглядит примерно так: `ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx`

### Шаг 2: Настройка через скрипт

**macOS/Linux:**
```bash
# Сделайте скрипт исполняемым
chmod +x setup-git-token.sh

# Запустите скрипт
./setup-git-token.sh

# Введите ваш GitHub username (например: Neeklo1606)
# Введите скопированный токен
```

**Windows:**
```cmd
# Просто запустите bat-файл
setup-git-token.bat

# Введите ваш GitHub username
# Введите скопированный токен
```

### Шаг 3: Ручная настройка (если скрипт не работает)

```bash
# Замените YOUR_USERNAME и YOUR_TOKEN на ваши данные
git remote set-url origin https://YOUR_USERNAME:YOUR_TOKEN@github.com/letoceiling-coder/avangard.git

# Пример:
# git remote set-url origin https://Neeklo1606:ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx@github.com/letoceiling-coder/avangard.git
```

### Шаг 4: Проверка

```bash
# Проверьте конфигурацию (токен будет скрыт)
git remote -v

# Попробуйте выполнить push (если есть изменения)
git push origin main

# Или просто проверьте доступ
git ls-remote origin
```

**✅ Готово!** Теперь вы можете выполнять деплой:
```bash
php artisan deploy --insecure
```

---

## 🔐 Вариант 2: Настройка через SSH (рекомендуется для постоянной работы)

### Шаг 1: Проверка существующих SSH ключей

```bash
# Проверьте, есть ли уже SSH ключи
ls -la ~/.ssh/id_*.pub

# Если ключи есть, переходите к Шагу 3
# Если ключей нет, переходите к Шагу 2
```

### Шаг 2: Создание нового SSH ключа

```bash
# Создайте новый SSH ключ (замените email на ваш)
ssh-keygen -t ed25519 -C "your_email@example.com"

# Нажмите Enter для всех вопросов (или укажите пароль для ключа)
# Ключ будет создан в: ~/.ssh/id_ed25519
```

### Шаг 3: Получение публичного ключа

```bash
# Покажите публичный ключ
cat ~/.ssh/id_ed25519.pub

# Или для RSA ключа:
# cat ~/.ssh/id_rsa.pub

# Скопируйте весь вывод (начинается с ssh-ed25519 или ssh-rsa)
```

### Шаг 4: Добавление ключа в GitHub

1. Откройте браузер и перейдите: https://github.com/settings/ssh/new
2. Заполните форму:
   - **Title**: `Avangard MacBook` (или любое название для идентификации компьютера)
   - **Key**: вставьте скопированный публичный ключ
3. Нажмите **"Add SSH key"**

### Шаг 5: Настройка через скрипт (автоматически)

```bash
# Сделайте скрипт исполняемым
chmod +x setup-git-ssh.sh

# Запустите скрипт
./setup-git-ssh.sh

# Скрипт покажет ваш публичный ключ (если нужно добавить в GitHub)
# Скрипт предложит переключить remote на SSH - ответьте 'y'
```

### Шаг 6: Ручная настройка (если скрипт не работает)

```bash
# Переключите remote на SSH
git remote set-url origin git@github.com:letoceiling-coder/avangard.git

# Проверьте подключение
ssh -T git@github.com

# Должно быть сообщение:
# Hi Neeklo1606! You've successfully authenticated, but GitHub does not provide shell access.
```

### Шаг 7: Проверка

```bash
# Проверьте конфигурацию
git remote -v

# Должно показать:
# origin  git@github.com:letoceiling-coder/avangard.git (fetch)
# origin  git@github.com:letoceiling-coder/avangard.git (push)

# Попробуйте выполнить push (если есть изменения)
git push origin main
```

**✅ Готово!** Теперь вы можете выполнять деплой:
```bash
php artisan deploy --insecure
```

---

## 🔧 Вариант 3: Получение прав на репозиторий

Если вы хотите работать без токенов и SSH, попросите владельца репозитория добавить вас как collaborator:

1. Владелец репозитория должен:
   - Перейти: https://github.com/letoceiling-coder/avangard/settings/access
   - Нажать **"Add people"**
   - Ввести ваш GitHub username
   - Выбрать права: **Write** или **Admin**
   - Подтвердить приглашение

2. Вы получите email-приглашение, примите его

3. После этого вы сможете работать с репозиторием через HTTPS с вашим паролем GitHub (или через SSH/Token)

---

## 🧪 Тестирование настройки

После настройки любого из вариантов, проверьте:

```bash
# 1. Проверка remote
git remote -v

# 2. Проверка доступа (для HTTPS)
git ls-remote origin

# 3. Проверка SSH (если используете SSH)
ssh -T git@github.com

# 4. Попробуйте сделать небольшое изменение и отправить
echo "# Test" >> TEST.md
git add TEST.md
git commit -m "Test commit"
git push origin main
git rm TEST.md
git commit -m "Remove test file"
git push origin main
```

---

## 🚀 Запуск деплоя

После успешной настройки доступа:

```bash
# Обычный деплой
php artisan deploy --insecure

# Деплой с пропуском проверки коммитов (если есть проблемы синхронизации)
php artisan deploy --insecure --skip-commit-check

# Деплой без сборки фронтенда (если уже собрано)
php artisan deploy --insecure --skip-build
```

---

## ❌ Решение проблем

### Проблема: "Permission denied" или "403 Forbidden"

**Решение:**
1. Проверьте правильность токена (для HTTPS)
2. Проверьте, что SSH ключ добавлен в GitHub (для SSH)
3. Убедитесь, что у вас есть права на репозиторий

### Проблема: "Host key verification failed"

**Решение:**
```bash
# Добавьте GitHub в known_hosts
ssh-keyscan github.com >> ~/.ssh/known_hosts
```

### Проблема: Токен не работает

**Решение:**
1. Проверьте срок действия токена в GitHub
2. Убедитесь, что токен имеет права `repo`
3. Создайте новый токен и обновите remote URL

### Проблема: SSH ключ не работает

**Решение:**
```bash
# Проверьте, что ключ добавлен в ssh-agent
eval "$(ssh-agent -s)"
ssh-add ~/.ssh/id_ed25519

# Проверьте подключение
ssh -T git@github.com
```

---

## 📝 Быстрая справка

### Команды для проверки:

```bash
# Текущая конфигурация remote
git remote -v

# Проверка доступа (HTTPS)
git ls-remote origin

# Проверка SSH
ssh -T git@github.com

# Текущая ветка
git branch

# Статус репозитория
git status
```

### Полезные ссылки:

- Создание токена: https://github.com/settings/tokens
- SSH ключи: https://github.com/settings/ssh
- Collaborators: https://github.com/letoceiling-coder/avangard/settings/access

---

## ✅ Чеклист настройки

- [ ] Репозиторий клонирован или склонирован
- [ ] Выбран вариант настройки (Token или SSH)
- [ ] Для Token: токен создан и добавлен в remote URL
- [ ] Для SSH: ключ создан, добавлен в GitHub, remote переключен на SSH
- [ ] Проверен доступ к репозиторию (`git ls-remote` или `ssh -T`)
- [ ] Выполнен тестовый push (опционально)
- [ ] Готов к деплою

---

**Дата создания:** Декабрь 2025  
**Версия:** 1.0

