# Итоги реализации функционала проверки актуальности данных

## ✅ Реализовано

### 1. База данных

#### Таблица `data_changes`
- Логирование всех изменений полей объектов Trend
- Полиморфная связь с объектами (Block, Parking, Village, CommercialBlock)
- Типизация изменений: `price`, `status`, `important`, `other`
- Хранение старых и новых значений
- Индексы для быстрого поиска

#### Таблица `price_history`
- История изменений цен (min, max, fixed)
- Автоматический расчет процента изменения
- Абсолютное изменение цены в копейках
- Полиморфная связь с объектами

### 2. Модели

#### `DataChange`
- Полиморфная связь с объектами
- Scopes для фильтрации по типу, периоду
- Методы для декодирования JSON значений

#### `PriceHistory`
- Полиморфная связь с объектами
- Scopes для фильтрации по типу цены, направлению изменения
- Автоматический расчет процента и суммы изменения
- Accessors для форматирования цен

#### `BaseTrendModel` (обновлен)
- Метод `scopeOutdated($days)` - поиск устаревших данных
- Метод `scopeNeedsSync($days)` - данные, требующие синхронизации
- Метод `isOutdated($days)` - проверка устарелости
- Метод `getDaysSinceLastSync()` - количество дней с последней синхронизации
- Полиморфные связи: `dataChanges()`, `priceHistory()`

### 3. Сервисы

#### `TrendDataSyncService` (улучшен)
- **Сравнение старых/новых значений** при синхронизации
- **Автоматическое логирование изменений** в `data_changes`
- **Логирование изменений цен** в `price_history`
- Опции: `track_changes`, `log_price_changes`
- Определение критичных и важных полей

**Методы:**
- `detectChanges()` - обнаружение изменений между старыми и новыми значениями
- `logChanges()` - логирование изменений в БД
- `logPriceChanges()` - логирование изменений цен
- `normalizeValue()` - нормализация значений для сравнения
- `getChangeType()` - определение типа изменения
- `serializeValue()` - сериализация значений для хранения

### 4. API Endpoints

#### Data Changes API (`/api/v1/data-changes`)
- `GET /api/v1/data-changes` - список изменений (с фильтрами)
- `GET /api/v1/data-changes/statistics` - статистика изменений
- `GET /api/v1/data-changes/{type}/{id}` - изменения конкретного объекта

**Фильтры:**
- `change_type` (price, status, important, other)
- `source` (parser, manual, feed, import)
- `field_name`
- `changeable_type`, `changeable_id`
- `date_from`, `date_to`

#### Price History API (`/api/v1/price-history`)
- `GET /api/v1/price-history` - история цен (с фильтрами)
- `GET /api/v1/price-history/statistics` - статистика изменений цен
- `GET /api/v1/price-history/{type}/{id}` - история цен конкретного объекта

**Фильтры:**
- `price_type` (min, max, fixed)
- `priceable_type`, `priceable_id`
- `source`
- `change_direction` (increase, decrease)
- `date_from`, `date_to`

#### Blocks API (обновлен)
- `GET /api/v1/blocks/outdated?days=7` - список устаревших блоков

**Параметры:**
- `days` - количество дней (по умолчанию 7)
- `per_page` - количество на странице

## 📊 Возможности

### 1. Отслеживание изменений
- ✅ Автоматическое логирование всех изменений при синхронизации
- ✅ Разделение по типам: цена, статус, важные, другие
- ✅ Хранение старых и новых значений
- ✅ Связь с пользователем (кто внес изменение)

### 2. История цен
- ✅ Отдельная таблица для истории цен
- ✅ Автоматический расчет процента и суммы изменения
- ✅ Разделение по типам: минимальная, максимальная, фиксированная цена
- ✅ Фильтрация по направлению: повышение/снижение

### 3. Определение устаревших данных
- ✅ Scope для поиска данных, не обновлявшихся N дней
- ✅ Метод проверки устарелости конкретного объекта
- ✅ API endpoint для получения списка устаревших объектов
- ✅ Информация о количестве дней с последней синхронизации

### 4. Статистика
- ✅ Статистика по типам изменений
- ✅ Статистика по полям
- ✅ Статистика изменений цен (повышения/снижения, средний процент)

## 🔧 Использование

### В коде синхронизации

```php
$syncService = new TrendDataSyncService();
$block = $syncService->syncBlock($apiData, [
    'track_changes' => true,        // Логировать изменения
    'log_price_changes' => true,    // Логировать изменения цен
]);
```

### Поиск устаревших данных

```php
// Блоки, не обновлявшиеся более 7 дней
$outdatedBlocks = Block::outdated(7)->get();

// Проверка конкретного блока
if ($block->isOutdated(7)) {
    $daysSince = $block->getDaysSinceLastSync();
    // ...
}
```

### Получение истории изменений

```php
// Все изменения блока
$changes = $block->dataChanges()->get();

// Изменения цен блока
$priceHistory = $block->priceHistory()->get();

// Только изменения цен (повышения)
$priceIncreases = PriceHistory::where('priceable_type', Block::class)
    ->where('priceable_id', $block->id)
    ->priceIncreases()
    ->get();
```

### API запросы

```bash
# Устаревшие блоки (более 7 дней)
GET /api/v1/blocks/outdated?days=7

# История изменений цен
GET /api/v1/price-history?priceable_type=App\Models\Trend\Block&priceable_id=1

# Статистика изменений
GET /api/v1/data-changes/statistics?date_from=2025-01-01
```

## 📝 Примечания

- Все изменения автоматически логируются при синхронизации через `TrendDataSyncService`
- Изменения цен логируются отдельно в `price_history` для удобства анализа
- Устаревшие данные определяются по полю `last_synced_at`
- Статистика доступна через API endpoints
- Все поля сохраняются с исходными типами (JSON для сложных типов)

