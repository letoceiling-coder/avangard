# ✅ Чеклист тестирования команд парсера

## 📋 Проверка структуры команды

- [x] Команда `trend:parse` зарегистрирована
- [x] Все опции команды определены корректно
- [x] Метод `handle()` реализован
- [x] Обработка ошибок присутствует
- [x] Логирование настроено

## 🔧 Проверка исправлений

- [x] Параметр `show_type` добавлен для blocks
- [x] Параметр `show_type` добавлен для commercial-blocks
- [x] Тип объекта передается в `buildParams()`
- [x] Отслеживание времени выполнения добавлено
- [x] Метод `formatExecutionTime()` реализован

## 🧪 Тестовые сценарии (требуют подключения к API)

### Базовые тесты

- [ ] Парсинг blocks для Москвы
  ```bash
  php artisan trend:parse --type=blocks --city=msk --limit=5
  ```
  Ожидается: успешное выполнение, статистика, время выполнения

- [ ] Парсинг blocks для Санкт-Петербурга
  ```bash
  php artisan trend:parse --type=blocks --city=spb --limit=5
  ```
  Ожидается: успешное выполнение с параметром show_type

- [ ] Парсинг commercial-blocks
  ```bash
  php artisan trend:parse --type=commercial-blocks --city=msk --limit=5
  ```
  Ожидается: успешное выполнение с параметром show_type

### Тесты с опциями

- [ ] С проверкой изображений
  ```bash
  php artisan trend:parse --type=blocks --city=msk --limit=5 --check-images
  ```
  Ожидается: проверка доступности изображений

- [ ] С принудительным обновлением
  ```bash
  php artisan trend:parse --type=blocks --city=msk --limit=5 --force
  ```
  Ожидается: обновление всех объектов

- [ ] С пропуском ошибок
  ```bash
  php artisan trend:parse --type=blocks --city=msk --limit=5 --skip-errors
  ```
  Ожидается: продолжение работы при ошибках

### Тесты нескольких типов

- [ ] Несколько типов объектов
  ```bash
  php artisan trend:parse --type=blocks --type=villages --city=msk --limit=5
  ```
  Ожидается: парсинг обоих типов последовательно

- [ ] Все типы объектов
  ```bash
  php artisan trend:parse --city=msk --limit=10
  ```
  Ожидается: парсинг всех поддерживаемых типов

### Тесты разных городов

- [ ] Москва (msk)
- [ ] Санкт-Петербург (spb)
- [ ] Краснодар (krasnodar)
- [ ] Ростов (rostov)

### Проверка времени выполнения

- [ ] Парсинг с лимитом 10 объектов (проверить время)
- [ ] Парсинг с лимитом 100 объектов (проверить время)
- [ ] Парсинг с лимитом 1000 объектов (проверить время)

### Проверка статистики

- [ ] Проверка вывода статистики в консоль
- [ ] Проверка корректности подсчета (создано/обновлено)
- [ ] Проверка логирования статистики

## 📊 Что проверить в результатах

### Консольный вывод

- [ ] Наличие сообщения о начале парсинга
- [ ] Успешная авторизация
- [ ] Информация о количестве городов
- [ ] Прогресс-бар для каждого типа объектов
- [ ] Итоговая статистика (таблица)
- [ ] Время выполнения

### База данных

- [ ] Данные сохраняются в соответствующие таблицы
- [ ] Поля заполняются корректно
- [ ] Связи создаются правильно
- [ ] Изображения сохраняются (если включена проверка)
- [ ] История изменений записывается

### Логи

- [ ] Логирование успешных операций
- [ ] Логирование ошибок (если есть)
- [ ] Логирование времени выполнения
- [ ] Корректность формата логов

## ⚠️ Известные проблемы для проверки

### 1. Паркинги - ObjectId вместо GUID

**Проблема:** API паркингов может требовать ObjectId  
**Тест:**
```bash
php artisan trend:parse --type=parkings --city=msk --limit=5
```
**Ожидается:** Проверить, требуется ли external_id или другой формат

### 2. Некоторые API могут не поддерживать все города

**Проблема:** villages, plots, commercial могут не работать для spb  
**Тест:**
```bash
php artisan trend:parse --type=villages --city=spb --limit=5
php artisan trend:parse --type=plots --city=spb --limit=5
```
**Ожидается:** Возможны ошибки 500 от API

### 3. Проверка параметра show_type

**Тест:** Убедиться, что параметр show_type передается в запрос  
**Проверка:** 
- Проверить логи запросов (если включены)
- Убедиться, что нет ошибок 400 о missing show_type

## 🎯 Критерии успешного теста

1. ✅ Команда выполняется без фатальных ошибок
2. ✅ Авторизация проходит успешно
3. ✅ Запросы к API выполняются
4. ✅ Данные сохраняются в БД
5. ✅ Статистика выводится корректно
6. ✅ Время выполнения отображается
7. ✅ Ошибки логируются (если есть)
8. ✅ Команда завершается с кодом 0

## 📝 Заметки для тестирования

- Используйте небольшие лимиты (5-10) для быстрого тестирования
- Проверяйте логи после каждого теста
- Сравнивайте результаты с предыдущими запусками
- Документируйте найденные проблемы

