# Реализация страницы управления регионами

## ✅ Что было реализовано:

### 1. Backend

#### Меню админ-панели (`app/Services/AdminMenu.php`)
- ✅ Добавлен пункт меню "Регионы" с иконкой `map-pin`
- ✅ Доступ только для роли `admin`

#### API Controller (`app/Http/Controllers/Api/RegionController.php`)
- ✅ `index()` - получение списка городов с регионами (дерево)
- ✅ `updateCity()` - обновление статуса активности города
- ✅ `updateRegion()` - обновление статуса активности региона
- ✅ `bulkUpdateCities()` - массовое обновление статусов городов
- ✅ `bulkUpdateRegions()` - массовое обновление статусов регионов

#### API Routes (`routes/api.php`)
- ✅ `GET /api/v1/regions` - список городов с регионами
- ✅ `PUT /api/v1/regions/cities/{city}` - обновление города
- ✅ `PUT /api/v1/regions/regions/{region}` - обновление региона
- ✅ `POST /api/v1/regions/cities/bulk-update` - массовое обновление городов
- ✅ `POST /api/v1/regions/regions/bulk-update` - массовое обновление регионов

#### Resources (`app/Http/Resources/CityResource.php`)
- ✅ Обновлен для включения регионов в ответе

#### Seeder (`database/seeders/RegionSeeder.php`)
- ✅ Создание/обновление городов и регионов:
  - Москва (активен)
  - Московская область (активна)
  - Санкт-Петербург (активен)
  - Ленинградская область (активна)
  - Ростов-на-Дону (активен)
  - Краснодар (активен)
  - Сочи (активен)
  - Екатеринбург (активен)
  - Новосибирск (активен)
  - Красноярск (активен)
  - Белгород (активен)

### 2. Frontend

#### Vue Route (`resources/js/admin.js`)
- ✅ Добавлен маршрут `/admin/regions` с компонентом `Regions.vue`
- ✅ Доступ только для роли `admin`

#### Vue Component (`resources/js/pages/admin/Regions.vue`)
- ✅ Древовидная структура: Города → Регионы
- ✅ Чекбоксы для управления активностью городов и регионов
- ✅ Разворачивание/сворачивание городов
- ✅ Счетчик активных регионов для каждого города
- ✅ Кнопка "Сохранить изменения" (доступна только при наличии изменений)
- ✅ Массовое сохранение всех изменений
- ✅ Состояния загрузки и ошибок
- ✅ Уведомления об успешном сохранении

### 3. База данных

- ✅ Все 11 городов и регионов созданы/обновлены
- ✅ Все города и регионы отмечены как активные (`is_active = true`)
- ✅ Правильная сортировка по `sort_order`

## 📋 Функциональность:

### Управление активностью:
- ✅ Включение/выключение городов чекбоксом
- ✅ Включение/выключение регионов чекбоксом
- ✅ Все изменения сохраняются одной кнопкой
- ✅ Визуальная индикация наличия изменений

### Удобство использования:
- ✅ Древовидная структура (Город → Регионы)
- ✅ Разворачивание/сворачивание городов
- ✅ Счетчик активных регионов
- ✅ Все города развернуты по умолчанию
- ✅ Адаптивный дизайн

## 🎯 Как использовать:

1. Перейти в админ-панель → Регионы
2. Выбрать нужные города и регионы (чекбоксы)
3. Нажать "Сохранить изменения"
4. Все изменения применятся к базе данных

## 📝 Примечания:

- Все города и регионы из списка уже созданы в базе данных
- Seeder можно запускать повторно для обновления данных
- API поддерживает как единичное, так и массовое обновление

