# 🚀 Быстрый старт: Настройка доступа к репозиторию

## Для нового компьютера (macOS/Linux)

### Вариант 1: Personal Access Token (5 минут) ⚡

```bash
# 1. Клонируйте репозиторий (если еще не клонирован)
git clone https://github.com/letoceiling-coder/avangard.git
cd avangard

# 2. Создайте токен в GitHub:
#    https://github.com/settings/tokens → Generate new token (classic)
#    Права: repo
#    Скопируйте токен!

# 3. Запустите скрипт настройки
chmod +x setup-git-token.sh
./setup-git-token.sh

# 4. Введите username и token
# 5. Готово! Можете деплоить:
php artisan deploy --insecure
```

### Вариант 2: SSH (10 минут) 🔐

```bash
# 1. Клонируйте репозиторий (если еще не клонирован)
git clone https://github.com/letoceiling-coder/avangard.git
cd avangard

# 2. Запустите скрипт настройки
chmod +x setup-git-ssh.sh
./setup-git-ssh.sh

# 3. Скрипт покажет публичный ключ - скопируйте его
# 4. Добавьте ключ в GitHub:
#    https://github.com/settings/ssh/new
# 5. Вернитесь в терминал, ответьте 'y' на вопрос о переключении remote
# 6. Готово! Можете деплоить:
php artisan deploy --insecure
```

---

## Для нового компьютера (Windows)

### Personal Access Token (5 минут) ⚡

```cmd
REM 1. Клонируйте репозиторий (если еще не клонирован)
git clone https://github.com/letoceiling-coder/avangard.git
cd avangard

REM 2. Создайте токен в GitHub:
REM    https://github.com/settings/tokens → Generate new token (classic)
REM    Права: repo
REM    Скопируйте токен!

REM 3. Запустите скрипт настройки
setup-git-token.bat

REM 4. Введите username и token
REM 5. Готово! Можете деплоить:
php artisan deploy --insecure
```

---

## 📖 Подробная инструкция

См. файл `GIT_SETUP_GUIDE.md` для полной инструкции со всеми деталями.

---

## ✅ Проверка настройки

```bash
# Проверьте remote
git remote -v

# Проверьте доступ
git ls-remote origin

# Если используете SSH:
ssh -T git@github.com
```

---

## ❌ Проблемы?

См. `DEPLOY_TROUBLESHOOTING.md` раздел "Проблема: Нет прав на запись в репозиторий"

