# Анализ API Endpoints TrendAgent.ru

Документ содержит детальный анализ всех API endpoints для парсинга данных TrendAgent.ru

**Дата создания:** 2025-01-28  
**Цель:** Подготовка документации для расширенного парсера с возможностью получения всех данных и фотографий

---

## 📋 Содержание

1. [Общие параметры](#общие-параметры)
2. [API Endpoints по категориям](#api-endpoints-по-категориям)
3. [Параметры запросов](#параметры-запросов)
4. [Фильтры](#фильтры)
5. [Структура данных](#структура-данных)
6. [Примеры использования](#примеры-использования)

---

## 🔧 Общие параметры

### Стандартные параметры (используются во всех запросах)

- **auth_token** (string, обязательный) - JWT токен авторизации, получается через SSO API
- **city** (string, обязательный) - ID города (например: `5a5cb42159042faa9a218d04` - Москва)
- **lang** (string, опциональный) - Язык ответа (`ru`, `en`), по умолчанию `ru`

### Стандартные домены API

- **api.trendagent.ru** - Основной API для квартир и блоков
- **parkings.trendagent.ru** - API для паркинга
- **house-api.trendagent.ru** - API для домов и участков
- **commerce.trendagent.ru** - API для коммерческой недвижимости
- **apartment-api.trendagent.ru** - API для квартир (дополнительные endpoints)
- **webinars-api.trendagent.ru** - API для вебинаров

---

## 🏢 API Endpoints по категориям

### 1. Квартиры (Apartments) - api.trendagent.ru/v4_29

#### 1.1 Поиск блоков (ЖК)

**Endpoint:** `GET /v4_29/blocks/search/`

**Описание:** Поиск жилых комплексов (блоков) с фильтрацией и сортировкой

**Параметры:**
- `show_type` (string) - Тип отображения:
  - `list` - Список (по умолчанию)
  - `map` - Карта
- `sort` (string) - Поле сортировки:
  - `price` - По цене
  - `start_sale_to` - По дате начала продаж
  - `name` - По названию
  - `deadline` - По сроку сдачи
- `sort_order` (string) - Порядок сортировки: `asc` | `desc`
- `count` (integer) - Количество результатов (1-100)
- `offset` (integer) - Смещение для пагинации (0, count, 2*count, ...)
- `room` (array) - Фильтр по количеству комнат:
  - `30` - Коттеджи (дома с участками)
  - `40` - Таунхаусы (дома с участками)
  - Можно передавать несколько значений: `room=30&room=40`

**Пример запроса:**
```
GET /v4_29/blocks/search/?show_type=list&sort=price&sort_order=asc&count=20&offset=0&city={city}&lang=ru&auth_token={token}
```

**Ответ:** JSON с полями:
- `data.results[]` - Массив объектов (блоков)
- `data.blocksCount` - Общее количество блоков
- `data.prelaunchesCount` - Количество прелаунчей
- `data.apartmentsCount` - Общее количество квартир
- `data.bookedApartmentsCount` - Забронировано квартир
- `data.viewApartmentsCount` - На просмотре квартир

#### 1.2 Поиск квартир

**Endpoint:** `GET /v4_29/apartments/search/`

**Описание:** Прямой поиск квартир (не блоков)

**Параметры:**
- `sort` (string) - Поле сортировки: `price` | `name` | `deadline`
- `sort_order` (string) - Порядок: `asc` | `desc`
- `count` (integer) - Количество результатов
- `offset` (integer) - Смещение

**Пример запроса:**
```
GET /v4_29/apartments/search/?sort=price&sort_order=asc&count=50&offset=0&city={city}&lang=ru&auth_token={token}
```

#### 1.3 Поиск прелаунчей

**Endpoint:** `GET /v4_29/prelaunches/search`

**Описание:** Поиск предварительных запусков (новостроек в стадии прелаунча)

**Параметры:**
- `sort` (string) - Поле сортировки: `price` | `name`
- `sort_order` (string) - Порядок: `asc` | `desc`

**Пример запроса:**
```
GET /v4_29/prelaunches/search?sort=price&sort_order=asc&city={city}&lang=ru&auth_token={token}
```

#### 1.4 Эксклюзивные объекты

**Endpoint:** `GET /v4_29/exclusives`

**Описание:** Получение списка эксклюзивных объектов

**Параметры:** Только стандартные (city, lang, auth_token)

**Пример запроса:**
```
GET /v4_29/exclusives?city={city}&lang=ru&auth_token={token}
```

**Ответ:** JSON с массивом эксклюзивных объектов, каждый содержит:
- `_id` - ID объекта
- `exclusive_priority` - Приоритет
- `title` - Заголовок
- `description` - Описание
- `image` - Изображение (path, file_name)
- `apartments_count` - Количество квартир
- `blocks_count` - Количество блоков
- `params` - Параметры (застройщик, список блоков)

#### 1.5 Справочники

**Endpoint:** `GET /v4_29/directories/rooms/`

**Описание:** Справочник типов квартир (студия, 1-к, 2-к и т.д.)

**Пример запроса:**
```
GET /v4_29/directories/rooms/?city={city}&lang=ru&auth_token={token}
```

**Endpoint:** `GET /v4_29/unit_measurements`

**Описание:** Единицы измерения

**Endpoint:** `GET /v4_29/tariffs/`

**Описание:** Тарифы

---

### 2. Паркинг (Parkings) - parkings.trendagent.ru

#### 2.1 Поиск парковочных мест

**Endpoint:** `GET /search/places/`

**Описание:** Поиск парковочных мест

**Параметры:**
- `count` (integer) - Количество результатов
- `offset` (integer) - Смещение
- `sort` (string) - Сортировка
- `sort_order` (string) - Порядок

**Пример запроса:**
```
GET /search/places/?count=20&offset=0&city={city}&lang=ru&auth_token={token}
```

#### 2.2 Справочники паркинга

**Endpoint:** `GET /enums/contract_types`

**Описание:** Типы договоров для паркинга

**Endpoint:** `GET /enums/parking_types`

**Описание:** Типы паркинга

**Endpoint:** `GET /enums/payment_types`

**Описание:** Типы оплаты для паркинга

**Endpoint:** `GET /enums/place_types`

**Описание:** Типы парковочных мест

**Endpoint:** `GET /directories/deadlines/`

**Описание:** Справочник сроков сдачи (для паркинга)

**Endpoint:** `GET /directories/sales_start/`

**Описание:** Справочник дат начала продаж (для паркинга)

---

### 3. Дома и участки (Houses/Plots) - house-api.trendagent.ru/v1

#### 3.1 Поиск поселков (участков)

**Endpoint:** `GET /v1/search/villages`

**Описание:** Поиск поселков с участками

**Параметры:**
- `count` (integer) - Количество результатов
- `offset` (integer) - Смещение
- `sort_type` (string) - Поле сортировки:
  - `price` - По цене
  - `start_sale_to` - По дате начала продаж
- `sort_order` (string) - Порядок: `asc` | `desc`

**Пример запроса:**
```
GET /v1/search/villages?count=15&sort_type=start_sale_to&sort_order=desc&city={city}&lang=ru&auth_token={token}
```

**Ответ:** JSON с полями:
- `list[]` - Массив поселков
- `total_count` - Общее количество поселков
- `result_count` - Количество в результате
- `plots_count` - Общее количество участков

#### 3.2 Фильтры для участков

**Endpoint:** `GET /v1/filter/plots`

**Описание:** Получение фильтров для участков

**Endpoint:** `GET /v1/filter/railway-stations`

**Описание:** Список железнодорожных станций (для фильтрации)

**Параметры:**
- `limit` (integer) - Лимит результатов
- `isJSON` (boolean) - Формат ответа JSON

**Endpoint:** `GET /v1/filter/escrow-banks`

**Описание:** Банки-эскроу (для фильтрации)

**Endpoint:** `GET /v1/filter`

**Описание:** Общие фильтры для домов/участков

#### 3.3 Эксклюзивные застройщики

**Endpoint:** `GET /v1/builders/exclusives`

**Описание:** Эксклюзивные застройщики для домов/участков

#### 3.4 Поиск проектов

**Endpoint:** `GET /v1/projects/search/`

**Описание:** Поиск проектов домов/участков

**Параметры:**
- `count` (integer) - Количество результатов

---

### 4. Коммерция (Commercial) - commerce.trendagent.ru

#### 4.1 Поиск блоков коммерции

**Endpoint:** `GET /search/blocks/`

**Описание:** Поиск коммерческих блоков (зданий)

**Параметры:**
- `show_type` (string) - Тип отображения: `list` | `map`
- `count` (integer) - Количество результатов
- `offset` (integer) - Смещение
- `sort` (string) - Поле сортировки:
  - `price` - По цене
  - `sales_start` - По дате начала продаж
- `sort_order` (string) - Порядок: `asc` | `desc`

**Пример запроса:**
```
GET /search/blocks/?show_type=list&count=15&sort=sales_start&sort_order=desc&city={city}&lang=ru&auth_token={token}
```

**Ответ:** JSON с полями:
- `result[]` - Массив коммерческих блоков
- `blocks_count` - Количество блоков
- `premises_count` - Количество помещений
- `booked_premises_count` - Забронировано помещений

#### 4.2 Поиск помещений

**Endpoint:** `GET /search/premises`

**Описание:** Прямой поиск коммерческих помещений

**Параметры:**
- `count` (integer) - Количество результатов
- `offset` (integer) - Смещение

#### 4.3 Фильтры коммерции

**Endpoint:** `GET /filters`

**Описание:** Получение всех фильтров для коммерции

**Параметры:** Множественные `name` (можно передавать несколько):
- `window_view_types` - Типы видов из окон
- `cardinals` - Стороны света
- `property_types` - Типы недвижимости
- `building_types` - Типы зданий
- `start_sales` - Даты начала продаж
- `deadline_keys` - Ключи сроков сдачи
- `entrances` - Количество входов
- `finishing_types` - Типы отделки
- `bathroom_types` - Типы санузлов
- `balconies_types` - Типы балконов
- `window_types` - Типы окон
- `piping_types` - Типы трубопроводов
- `levels` - Этажи
- `ventilation_types` - Типы вентиляции
- `payment_types` - Типы оплаты
- `banks` - Банки
- `contract_types` - Типы договоров
- `buyer_requirements` - Требования к покупателю
- `deadlines` - Сроки сдачи
- `purposes` - Назначения помещений
- `ceiling_heights` - Высоты потолков
- `level_types` - Типы этажей
- `distance_subways` - Расстояния до метро
- `nearby_places` - Близлежащие места

**Пример запроса:**
```
GET /filters?name=property_types&name=building_types&name=finishing_types&...&city={city}&lang=ru&auth_token={token}
```

---

### 5. Квартиры (дополнительный API) - apartment-api.trendagent.ru/v1

#### 5.1 Справочники для квартир

**Endpoint:** `GET /v1/directories`

**Описание:** Получение справочников для фильтрации квартир

**Параметры:** Множественные `types` (можно передавать несколько):
- `banks` - Банки
- `subway_distances` - Расстояния до метро
- `distance_points` - Точки расстояний
- `locations` - Локации
- `nearby_place_types` - Типы близлежащих мест
- `regions` - Районы
- `subways` - Метро

**Пример запроса:**
```
GET /v1/directories?types=banks&types=subways&types=regions&city={city}&lang=ru&auth_token={token}
```

---

### 6. Вебинары - webinars-api.trendagent.ru/v1

#### 6.1 Типы вебинаров

**Endpoint:** `GET /v1/webinar_types`

**Описание:** Получение типов вебинаров

#### 6.2 События (вебинары)

**Endpoint:** `GET /v1/events`

**Описание:** Получение списка вебинаров/событий

**Параметры:**
- `date_from` (datetime) - Дата начала в формате ISO 8601: `2025-12-28T00:00:00`
- `date_to` (datetime) - Дата окончания: `2025-12-28T23:59:59`

**Пример запроса:**
```
GET /v1/events?city={city}&date_from=2025-12-28T00:00:00&date_to=2025-12-28T23:59:59&lang=ru&auth_token={token}
```

---

## 🔍 Параметры запросов

### Параметры пагинации

| Параметр | Тип | Описание | Значения по умолчанию |
|----------|-----|----------|----------------------|
| `count` | integer | Количество результатов на страницу | 20 |
| `offset` | integer | Смещение для пагинации | 0 |
| `page` | integer | Номер страницы (альтернатива offset) | 1 |

**Расчет offset из page:** `offset = (page - 1) * count`

### Параметры сортировки

| Параметр | Тип | Описание | Возможные значения |
|----------|-----|----------|-------------------|
| `sort` | string | Поле для сортировки | `price`, `name`, `deadline`, `start_sale_to`, `sales_start` |
| `sort_order` | string | Направление сортировки | `asc` (по возрастанию), `desc` (по убыванию) |
| `sort_type` | string | Поле для сортировки (для участков) | `price`, `start_sale_to` |

### Параметры фильтрации

#### Фильтр по типу объекта (room)

Параметр `room` используется для фильтрации по типу объекта:

- `room=30` - Коттеджи (дома с участками)
- `room=40` - Таунхаусы (дома с участками)
- Можно передавать несколько: `room=30&room=40`

**Важно:** Для квартир параметр `room` НЕ передается (или передается пустой массив)

#### Фильтр по типу отображения (show_type)

- `list` - Список объектов
- `map` - Отображение на карте

---

## 🖼️ Работа с изображениями

### Формат URL изображений

Все изображения хранятся на CDN: `https://selcdn.trendagent.ru/images/`

**Структура URL:**
```
https://selcdn.trendagent.ru/images/{path}/m_{file_name}
```

Где:
- `{path}` - Путь к изображению (например: `w0/wu/`)
- `m_{file_name}` - Имя файла с префиксом `m_` для миниатюры
- Без префикса `m_` - полное изображение

**Пример:**
- Миниатюра: `https://selcdn.trendagent.ru/images/w0/wu/m_250ea8a64f4cadf7c24dd727674c0e4a.png`
- Полное: `https://selcdn.trendagent.ru/images/w0/wu/250ea8a64f4cadf7c24dd727674c0e4a.png`

### Структура объекта изображения в API ответах

```json
{
  "image": {
    "_id": "63c00c0b9a85d5af16f5804c",
    "path": "w0/wu/",
    "file_name": "250ea8a64f4cadf7c24dd727674c0e4a.png"
  }
}
```

### Для паркинга (массив изображений)

```json
{
  "images": [
    {
      "path": "w0/wu/",
      "file_name": "image1.png"
    },
    {
      "path": "w0/wu/",
      "file_name": "image2.png"
    }
  ]
}
```

---

## 📊 Структура данных

### Блок (ЖК) - blocks/search/

```json
{
  "_id": "61b1a7bbcc0662b03203375f",
  "name": "Название ЖК",
  "address": ["ул. Примерная, 1"],
  "builder": {
    "_id": "60a1b2c3d4e5f6a7b8c9d0e1",
    "name": "Название застройщика"
  },
  "region": {
    "_id": "60a1b2c3d4e5f6a7b8c9d0e2",
    "name": "Название района"
  },
  "image": {
    "path": "w0/wu/",
    "file_name": "image.png"
  },
  "min_price": 5000000,
  "max_price": 15000000,
  "apart_count": 500,
  "view_apart_count": 50,
  "deadline": "2025-12-31",
  "finishing": "Чистовая",
  "reward": [
    {
      "label": "Комиссия",
      "value": "2%"
    }
  ]
}
```

### Эксклюзивный объект - exclusives

```json
{
  "_id": "61b1a7bbcc0662b03203375f",
  "exclusive_priority": 1,
  "rolled": false,
  "blocks_count": 16,
  "is_top": true,
  "title": "Эксклюзивно на TrendAgent",
  "description": "Описание эксклюзива",
  "image": {
    "_id": "63c00c0b9a85d5af16f5804c",
    "path": "w0/wu/",
    "file_name": "image.png"
  },
  "apartments_count": 587,
  "params": {
    "_id": "63c005d328d3bc32d0077a4c",
    "name": "FORMA",
    "blocks": [
      {
        "_id": "61b1a7bbcc0662b03203375f",
        "name": "FORST"
      }
    ]
  }
}
```

---

## 📝 Примеры использования

### Пример 1: Поиск квартир с пагинацией

```php
// Страница 1 (первые 20 результатов)
GET /v4_29/blocks/search/?show_type=list&sort=price&sort_order=asc&count=20&offset=0&city={city}&lang=ru&auth_token={token}

// Страница 2 (следующие 20 результатов)
GET /v4_29/blocks/search/?show_type=list&sort=price&sort_order=asc&count=20&offset=20&city={city}&lang=ru&auth_token={token}

// Страница 3
GET /v4_29/blocks/search/?show_type=list&sort=price&sort_order=asc&count=20&offset=40&city={city}&lang=ru&auth_token={token}
```

### Пример 2: Поиск домов с участками (room=30,40)

```php
GET /v4_29/blocks/search/?show_type=list&count=15&sort=start_sale_to&sort_order=desc&room=30&room=40&city={city}&lang=ru&auth_token={token}
```

### Пример 3: Получение фильтров для коммерции

```php
GET /filters?name=property_types&name=building_types&name=finishing_types&name=payment_types&name=banks&city={city}&lang=ru&auth_token={token}
```

### Пример 4: Поиск участков

```php
GET /v1/search/villages?count=15&sort_type=start_sale_to&sort_order=desc&city={city}&lang=ru&auth_token={token}
```

---

## 🔄 Дубликаты в исходных ссылках

### Найденные дубликаты:

1. **blocks/search с show_type=map** - дублируется 2 раза (строки 5, 6)
2. **apartments/search** - дублируется 2 раза с разными count (30 и 50) - строки 3, 4
3. **blocks/search с show_type=list, sort=start_sale_to** - дублируется (строка 7, 18, но во второй есть room=30&room=40)

### Уникальные endpoints:

Всего уникальных endpoints в списке: ~30

---

## 🎯 Рекомендации для парсера

### 1. Структура классов

Создать отдельные классы для каждого типа объектов:
- `TrendApartmentsParser` - для квартир
- `TrendParkingsParser` - для паркинга
- `TrendHousesParser` - для домов/участков
- `TrendCommercialParser` - для коммерции

### 2. Обработка изображений

Создать метод для формирования URL изображений:
```php
public function getImageUrl(array $image, bool $thumbnail = true): string
{
    $path = rtrim($image['path'], '/');
    $path = ltrim($path, '/');
    $fileName = $image['file_name'];
    $prefix = $thumbnail ? 'm_' : '';
    return "https://selcdn.trendagent.ru/images/{$path}/{$prefix}{$fileName}";
}
```

### 3. Пагинация

Реализовать автоматическую пагинацию для получения всех данных:
```php
public function getAllResults(array $params = []): array
{
    $allResults = [];
    $offset = 0;
    $count = $params['count'] ?? 20;
    
    do {
        $params['offset'] = $offset;
        $response = $this->search($params);
        $results = $response['data']['results'] ?? [];
        $allResults = array_merge($allResults, $results);
        $offset += $count;
    } while (count($results) >= $count);
    
    return $allResults;
}
```

### 4. Кэширование справочников

Справочники (directories, enums, filters) не меняются часто - кэшировать их результаты.

### 5. Обработка ошибок

- Проверять срок действия токена
- Обрабатывать ошибки 401 (неавторизован)
- Обрабатывать ошибки 429 (слишком много запросов) с задержкой
- Логировать все запросы и ответы

---

## 📌 Важные замечания

1. **Токен авторизации:** Все запросы требуют валидный JWT токен в параметре `auth_token`
2. **Город:** Параметр `city` обязателен во всех запросах
3. **Множественные параметры:** Некоторые параметры можно передавать несколько раз (например: `types=banks&types=subways`)
4. **Лимиты:** Максимальное количество результатов в одном запросе обычно 100
5. **Формат дат:** Даты в формате ISO 8601: `2025-12-28T00:00:00`
6. **Кодировка:** Все URL параметры должны быть правильно закодированы (urlencode)

---

**Документ подготовлен для разработки расширенного парсера TrendAgent.ru API**

