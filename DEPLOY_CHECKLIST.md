# Чеклист проверки деплоя

## ✅ Выполнено

1. ✅ Команда деплоя создана: `php artisan deploy`
2. ✅ Скрипт `build:all` добавлен в package.json
3. ✅ Зависимости установлены (laravel-vite-plugin, vuex, @tailwindcss/vite)
4. ✅ Конфигурация frontend/vite.config.ts обновлена для сборки в public/frontend
5. ✅ Git репозиторий настроен и подключен

## ✅ Исправлено

1. ✅ Проблема с Tailwind CSS v4 решена - откат на v3
2. ✅ Все зависимости установлены (vuex, vue-router, axios, sweetalert2, fslightbox-vue, vue-advanced-cropper)
3. ✅ Сборка работает успешно

## ⚠️ Требует внимания

1. ⚠️ Нужно добавить в .env:
   - `DEPLOY_SERVER_URL=https://ваш-сервер.com`
   - `DEPLOY_TOKEN=ваш-секретный-токен`

## 📋 Процесс деплоя

### Локально (перед деплоем):

1. Убедитесь, что все зависимости установлены:
   ```bash
   npm install
   cd frontend && npm install && cd ..
   composer install
   ```

2. Проверьте сборку (dry-run):
   ```bash
   php artisan deploy --dry-run
   ```

3. Выполните деплой:
   ```bash
   php artisan deploy --message "Описание изменений"
   ```

### Что делает команда деплоя:

1. **Сборка фронтенда** (`npm run build:all`):
   - Собирает Vue админку в `public/build`
   - Собирает React приложение в `public/frontend`

2. **Git операции**:
   - Проверяет статус git
   - Добавляет все изменения
   - Создает коммит
   - Отправляет в репозиторий

3. **Отправка на сервер**:
   - Отправляет POST запрос на `/api/deploy`
   - Сервер выполняет:
     - `git pull`
     - `composer install`
     - `php artisan migrate`
     - Очистка кешей
     - Оптимизация

## 🔧 Настройка на сервере

1. Убедитесь, что на сервере настроены:
   - `DEPLOY_TOKEN` в .env (должен совпадать с локальным)
   - PHP путь (или `PHP_PATH` в .env)
   - Composer (или `COMPOSER_PATH` в .env)

2. Проверьте middleware `VerifyDeployToken` в `app/Http/Middleware/VerifyDeployToken.php`

## 🐛 Известные проблемы

1. **Windows PowerShell**: Используйте `;` вместо `&&` в некоторых командах (но npm скрипты работают с `&&`)

## 📝 Следующие шаги

1. ✅ Сборка работает - можно выполнять деплой
2. Добавить настройки в .env (DEPLOY_SERVER_URL и DEPLOY_TOKEN)
3. Протестировать полный цикл деплоя на реальном сервере

