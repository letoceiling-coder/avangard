# Telegram Mini App - Backend система

Backend система для Telegram Mini App приложений на базе Laravel 11. Полнофункциональная платформа с админ-панелью, системой поддержки, медиа-библиотекой и автоматическим деплоем.

## 🎯 Основные возможности

### 🔐 Система авторизации
- Регистрация и вход пользователей
- Восстановление пароля
- Защита API через Laravel Sanctum
- JWT токены для аутентификации

### 👨‍💼 Админ-панель
- Современный интерфейс на Vue 3 + TypeScript
- Адаптивный дизайн
- Система ролей и прав доступа (admin, manager, user)
- Динамическое меню с фильтрацией по ролям

### 📁 Медиа-библиотека
- Организация файлов в папки с древовидной структурой
- Поддержка различных типов файлов (изображения, документы, видео)
- Загрузка файлов через drag & drop
- Корзина для удаленных файлов с возможностью восстановления
- Фильтрация и поиск файлов
- Управление позициями папок

### 🔔 Система уведомлений
- Внутренние уведомления для пользователей админ-панели
- Отметка уведомлений как прочитанных
- Подсчет непрочитанных уведомлений
- История всех уведомлений

### 🎫 Система поддержки
- Создание тикетов с прикреплением файлов
- Обмен сообщениями в тикетах
- Интеграция с внешними CRM системами через webhooks
- Получение сообщений и изменение статусов от внешних систем
- Защита webhook'ов токеном

### 👥 Управление пользователями и ролями
- CRUD операции для пользователей
- Система ролей (admin, manager, user)
- Назначение ролей пользователям
- Фильтрация доступа к разделам админ-панели по ролям

### 💳 Проверка подписки
- Интеграция с внешними системами управления подписками
- Проверка статуса подписки через API
- Кеширование результатов проверки
- Блокировка доступа при истечении подписки

### 🚀 Автоматический деплой
- **GitHub Actions** - автоматический деплой при push в main
- **GitHub Webhook** - обработка push событий от GitHub
- **Git Hooks** - автоматический деплой при локальном push
- Обновление кода, миграций, оптимизация приложения

### 📱 Telegram интеграция
- Пакет `letoceiling-coder/telegram` для работы с Telegram Bot API
- Валидация Telegram initData
- Интеграция с Telegram Mini Apps

### 🤖 Управление Telegram ботами (v1.1.0+)
- Регистрация и управление Telegram ботами через админ-панель
- Автоматическая регистрация webhook при создании бота
- Настройка приветственного сообщения для ботов
- Проверка установки webhook через Telegram API
- Настройка параметров webhook (allowed_updates, max_connections, secret_token)
- Обработка команды /start с автоматической отправкой приветственного сообщения
- Подробное логирование всех операций с ботами
- Публичный endpoint для приема webhook от Telegram

### 📋 Полнофункциональный бот АИП (v1.2.0+)
- Полный функционал чат-бота для Аудиторско-консалтинговой группы «АИП»
- Система проверки подписки на обязательный Telegram-канал
- Управление заявками на консультации через админ-панель
- Категории материалов с поддержкой внешних ссылок (Mini App)
- Управление материалами для скачивания (загрузка, медиа-библиотека, URL, Telegram file_id)
- Настройка всех текстов сообщений бота через админ-панель
- Уведомления администраторам о новых заявках
- Статистика работы бота (пользователи, заявки, скачивания материалов)
- FSM (Finite State Machine) для управления состояниями пользователей
- Динамическая валидация полей формы на основе настроек бота

## 🛠 Технологический стек

**Backend:**
- Laravel 11
- PHP 8.2+
- MySQL/PostgreSQL
- Laravel Sanctum (авторизация)

**Frontend:**
- Vue 3 (админ-панель)
- TypeScript
- Vite (сборка)
- TailwindCSS

**Интеграции:**
- Telegram Bot API (через `letoceiling-coder/telegram`)
- Telegram Mini App API

**DevOps:**
- GitHub Actions
- Git Hooks
- Автоматический деплой

## 📁 Структура проекта

```
project/
├── app/
│   ├── Http/
│   │   ├── Controllers/
│   │   │   ├── Api/              # API контроллеры
│   │   │   │   ├── Admin/        # Контроллеры админ-панели
│   │   │   │   ├── BotController.php # Управление ботами (v1.1.0+)
│   │   │   │   └── v1/           # Версионированные контроллеры
│   │   │   └── LogController.php # Просмотр логов
│   │   ├── Middleware/           # Middleware
│   │   └── Requests/             # Валидация запросов
│   ├── Models/                   # Eloquent модели
│   │   ├── Bot.php              # Модель ботов (v1.1.0+)
│   │   ├── Folder.php           # Модель папок
│   │   ├── Media.php            # Модель медиа-файлов
│   │   ├── Notification.php     # Модель уведомлений
│   │   ├── Role.php             # Модель ролей
│   │   ├── SupportTicket.php    # Модель тикетов
│   │   └── User.php             # Модель пользователей
│   ├── Services/                 # Бизнес-логика
│   │   ├── AdminMenu.php        # Генерация меню админ-панели
│   │   ├── SubscriptionService.php # Проверка подписки
│   │   ├── SupportService.php   # Логика поддержки
│   │   ├── TelegramService.php  # Работа с Telegram Bot API (v1.1.0+)
│   │   └── Integration/         # Интеграции с внешними системами
│   ├── Console/
│   │   └── Commands/            # Artisan команды
│   └── Providers/               # Service Providers
├── database/
│   ├── migrations/              # Миграции БД
│   └── seeders/                # Seeders
├── resources/
│   ├── js/
│   │   ├── pages/admin/        # Страницы админ-панели
│   │   ├── components/         # Vue компоненты
│   │   └── admin.js            # Точка входа Vue приложения
│   └── views/
│       ├── admin.blade.php     # Шаблон админ-панели
│       └── react.blade.php     # Шаблон пользовательского приложения
├── routes/
│   ├── api.php                 # API маршруты
│   ├── web.php                 # Web маршруты
│   └── console.php             # Консольные команды
├── .github/
│   └── workflows/
│       └── deploy.yml          # GitHub Actions для деплоя
└── public/                     # Публичная директория
```

## 🚀 Установка

### Требования

- **PHP** 8.2 или выше
- **Composer** 2.0 или выше
- **Node.js** 18.0 или выше (рекомендуется 20.x)
- **npm** или **yarn**
- **MySQL** 8.0+ или **PostgreSQL** 13+
- **Веб-сервер** (Apache 2.4+ или Nginx 1.18+)
- **SSL-сертификат** (обязательно для Telegram Mini App)

### Установка на локальной машине (разработка)

#### Шаг 1: Клонирование репозитория

```bash
# Клонировать последнюю версию из main
git clone https://github.com/letoceiling-coder/avangard.git
cd avangard

# ИЛИ клонировать конкретную стабильную версию
git clone --branch v1.0.0 --depth 1 https://github.com/letoceiling-coder/avangard.git
cd avangard
```

**💡 Установка конкретной версии:**
Если вам нужна конкретная стабильная версия, используйте git теги. См. подробную документацию в [VERSIONING.md](VERSIONING.md).

```bash
# В существующем репозитории переключиться на версию
git fetch --tags
git checkout v1.0.0
```

#### Шаг 2: Установка зависимостей PHP

```bash
# Установить зависимости Composer
composer install

# Для production используйте:
# composer install --no-dev --optimize-autoloader
```

#### Шаг 3: Установка зависимостей Node.js

```bash
# Установить зависимости npm
npm install

# ИЛИ используйте yarn
yarn install
```

#### Шаг 4: Настройка окружения

```bash
# Скопировать файл окружения
cp .env.example .env

# Сгенерировать ключ приложения
php artisan key:generate
```

#### Шаг 5: Настройка базы данных

1. **Создайте базу данных** в MySQL или PostgreSQL:

```sql
-- MySQL
CREATE DATABASE your_database_name CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- PostgreSQL
CREATE DATABASE your_database_name;
```

2. **Настройте подключение в `.env`:**

```env
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=your_database_name
DB_USERNAME=your_username
DB_PASSWORD=your_password
```

Для PostgreSQL:
```env
DB_CONNECTION=pgsql
DB_HOST=127.0.0.1
DB_PORT=5432
DB_DATABASE=your_database_name
DB_USERNAME=your_username
DB_PASSWORD=your_password
```

#### Шаг 6: Выполнение миграций

```bash
# Выполнить миграции базы данных
php artisan migrate

# Если нужно выполнить миграции без подтверждения (для production)
php artisan migrate --force
```

#### Шаг 7: Создание первого пользователя и роли

После установки необходимо создать роль `admin` и назначить её первому пользователю.

**Вариант 1: Через Laravel Tinker (рекомендуется)**

```bash
php artisan tinker
```

Затем выполните в tinker:

```php
// Создать роль admin
$role = \App\Models\Role::firstOrCreate(
    ['slug' => 'admin'],
    [
        'name' => 'Администратор',
        'description' => 'Полный доступ к системе'
    ]
);

// Создать первого пользователя (замените данные на свои)
$user = \App\Models\User::create([
    'name' => 'Администратор',
    'email' => 'admin@example.com',
    'password' => \Illuminate\Support\Facades\Hash::make('your_secure_password'),
]);

// Назначить роль пользователю
$user->roles()->attach($role->id);

// Проверить
echo "Пользователь создан: " . $user->email . "\n";
echo "Роли: " . $user->roles->pluck('slug')->implode(', ') . "\n";
exit
```

**Вариант 2: Через SQL напрямую**

```sql
-- Создать роль admin
INSERT INTO roles (slug, name, description, created_at, updated_at) 
VALUES ('admin', 'Администратор', 'Полный доступ к системе', NOW(), NOW())
ON DUPLICATE KEY UPDATE name='Администратор';

-- Создать пользователя (пароль: your_secure_password)
INSERT INTO users (name, email, password, created_at, updated_at)
VALUES ('Администратор', 'admin@example.com', '$2y$10$92IXUNpkjO0rOQ5byMi.Ye4oKoEa3Ro9llC/.og/at2.uheWG/igi', NOW(), NOW());

-- Назначить роль пользователю (замените USER_ID и ROLE_ID на реальные значения)
INSERT INTO role_user (role_id, user_id) 
SELECT r.id, u.id 
FROM roles r, users u 
WHERE r.slug = 'admin' AND u.email = 'admin@example.com';
```

**⚠️ ВАЖНО:** После создания пользователя через SQL обязательно измените пароль через админ-панель или создайте нового пользователя с правильным хешем пароля.

#### Шаг 8: Настройка Telegram (опционально)

Если планируете использовать управление ботами через админ-панель:

```env
TELEGRAM_BOT_TOKEN=your_bot_token_here
```

#### Шаг 9: Настройка системы подписки (опционально)

```env
SUBSCRIPTION_API_URL=https://crm.siteaccess.ru/api/v1/subscription/check
SUBSCRIPTION_API_TOKEN=your_subscription_api_token
```

#### Шаг 10: Сборка фронтенда

```bash
# Собрать админ-панель
npm run build:admin

# ИЛИ для разработки с hot-reload
npm run dev:admin
```

#### Шаг 11: Настройка веб-сервера

**Apache (.htaccess уже настроен):**

Убедитесь, что в `public/.htaccess` есть правила для Laravel:

```apache
<IfModule mod_rewrite.c>
    <IfModule mod_negotiation.c>
        Options -MultiViews -Indexes
    </IfModule>

    RewriteEngine On

    # Handle Authorization Header
    RewriteCond %{HTTP:Authorization} .
    RewriteRule .* - [E=HTTP_AUTHORIZATION:%{HTTP:Authorization}]

    # Redirect Trailing Slashes If Not A Folder...
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteCond %{REQUEST_URI} (.+)/$
    RewriteRule ^ %1 [L,R=301]

    # Send Requests To Front Controller...
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteRule ^ index.php [L]
</IfModule>
```

**Nginx:**

```nginx
server {
    listen 80;
    server_name your-domain.com;
    root /path/to/avangard/public;

    add_header X-Frame-Options "SAMEORIGIN";
    add_header X-Content-Type-Options "nosniff";

    index index.php;

    charset utf-8;

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location = /favicon.ico { access_log off; log_not_found off; }
    location = /robots.txt  { access_log off; log_not_found off; }

    error_page 404 /index.php;

    location ~ \.php$ {
        fastcgi_pass unix:/var/run/php/php8.2-fpm.sock;
        fastcgi_param SCRIPT_FILENAME $realpath_root$fastcgi_script_name;
        include fastcgi_params;
    }

    location ~ /\.(?!well-known).* {
        deny all;
    }
}
```

#### Шаг 12: Настройка прав доступа

```bash
# Установить права на запись для storage и cache
chmod -R 775 storage bootstrap/cache

# Создать симлинк для storage (если нужно)
php artisan storage:link
```

### Установка на production сервере (Beget и другие)

#### Подготовка сервера

1. **Подключитесь к серверу по SSH**

2. **Перейдите в директорию проекта:**

```bash
cd ~/your-domain.com/public_html
```

3. **Клонируйте проект:**

```bash
# Если директория пустая
git clone --branch v1.0.0 --depth 1 https://github.com/letoceiling-coder/avangard.git .

# ИЛИ если директория не пустая
git clone --branch v1.0.0 --depth 1 https://github.com/letoceiling-coder/avangard.git temp_clone
mv temp_clone/* temp_clone/.[^.]* . 2>/dev/null || true
rmdir temp_clone
```

4. **Установите зависимости:**

```bash
# PHP зависимости (production режим)
composer install --no-dev --optimize-autoloader

# Node.js зависимости
cd frontend
npm install
cd ..
```

5. **Настройте `.env`:**

```bash
cp .env.example .env
# Отредактируйте .env файл с настройками вашего сервера
nano .env
```

Обязательные настройки в `.env`:
```env
APP_ENV=production
APP_DEBUG=false
APP_URL=https://your-domain.com

DB_CONNECTION=mysql
DB_HOST=localhost
DB_DATABASE=your_database
DB_USERNAME=your_username
DB_PASSWORD=your_password
```

6. **Сгенерируйте ключ приложения:**

```bash
php artisan key:generate
```

7. **Выполните миграции:**

```bash
php artisan migrate --force
```

8. **Создайте первого пользователя и роль** (см. Шаг 7 выше)

9. **Соберите фронтенд:**

```bash
cd frontend
npm run build
cd ..
```

10. **Очистите кеши:**

```bash
php artisan config:clear
php artisan cache:clear
php artisan route:clear
php artisan view:clear
php artisan optimize
```

11. **Настройте права доступа:**

```bash
# На shared hosting может не работать chown, это нормально
chmod -R 755 storage bootstrap/cache

# Создать симлинк для storage
php artisan storage:link
```

### Решение типичных проблем

#### Проблема: "vite: Permission denied" при сборке

```bash
cd frontend
chmod +x node_modules/.bin/vite
npm run build
# ИЛИ используйте npx
npx vite build
```

#### Проблема: Бесконечный редирект после авторизации

Убедитесь, что у пользователя назначена роль `admin`. См. Шаг 7 выше.

#### Проблема: "Class 'App\Models\Role' not found"

Убедитесь, что миграции выполнены:
```bash
php artisan migrate --force
```

#### Проблема: "No application encryption key has been specified"

```bash
php artisan key:generate
```

#### Проблема: Ошибки при выполнении миграций

```bash
# Проверьте подключение к БД в .env
# Выполните миграции с выводом ошибок
php artisan migrate --force -vvv
```

#### Проблема: Фронтенд не собирается

```bash
cd frontend
# Очистить кеш npm
npm cache clean --force
# Удалить node_modules и переустановить
rm -rf node_modules package-lock.json
npm install
npm run build
```

### Проверка установки

После установки проверьте:

1. **Доступность сайта:** Откройте `https://your-domain.com` в браузере
2. **Админ-панель:** Откройте `https://your-domain.com/admin` и войдите с созданным пользователем
3. **API:** Проверьте доступность API через `https://your-domain.com/api/auth/user` (требует авторизации)

### Дополнительная настройка

#### Настройка деплоя (опционально)

```env
DEPLOY_TOKEN=your_deploy_token
DEPLOY_SERVER_URL=https://your-domain.com
GITHUB_WEBHOOK_SECRET=your_webhook_secret
```

См. раздел "Автоматический деплой" ниже для подробной настройки.

## 📚 API Документация

### Авторизация

Все защищенные endpoints требуют Bearer токен в заголовке `Authorization`:

```
Authorization: Bearer {token}
```

Токен получается после успешного входа через `/api/auth/login`.

### Основные endpoints

#### Авторизация
- `POST /api/auth/register` - Регистрация
- `POST /api/auth/login` - Вход
- `POST /api/auth/logout` - Выход
- `GET /api/auth/user` - Текущий пользователь
- `POST /api/auth/forgot-password` - Запрос восстановления пароля
- `POST /api/auth/reset-password` - Сброс пароля

#### Медиа-библиотека
- `GET /api/v1/folders` - Список папок
- `POST /api/v1/folders` - Создать папку
- `GET /api/v1/folders/tree/all` - Дерево папок
- `GET /api/v1/media` - Список медиа-файлов
- `POST /api/v1/media` - Загрузить файл

#### Управление пользователями (только admin)
- `GET /api/v1/users` - Список пользователей
- `POST /api/v1/users` - Создать пользователя
- `GET /api/v1/roles` - Список ролей

#### Система поддержки (только admin)
- `GET /api/v1/support/tickets` - Список тикетов
- `POST /api/v1/support/ticket` - Создать тикет
- `POST /api/v1/support/message` - Отправить сообщение

#### Управление Telegram ботами (только admin, v1.1.0+)
- `GET /api/v1/bots` - Список ботов
- `POST /api/v1/bots` - Создать бота (автоматически регистрирует webhook)
- `GET /api/v1/bots/{id}` - Информация о боте
- `PUT /api/v1/bots/{id}` - Обновить бота
- `DELETE /api/v1/bots/{id}` - Удалить бота
- `GET /api/v1/bots/{id}/check-webhook` - Проверить установку webhook
- `POST /api/v1/bots/{id}/register-webhook` - Зарегистрировать webhook
- `POST /api/telegram/webhook/{id}` - Публичный endpoint для приема обновлений от Telegram

#### Управление ботом АИП (только admin, v1.2.0+)
- `GET /api/v1/bot-management/{botId}/consultations` - Список заявок на консультации
- `GET /api/v1/bot-management/{botId}/consultations/{id}` - Детали заявки
- `PUT /api/v1/bot-management/{botId}/consultations/{id}` - Обновить заявку
- `GET /api/v1/bot-management/{botId}/settings` - Настройки бота
- `PUT /api/v1/bot-management/{botId}/settings` - Обновить настройки бота
- `GET /api/v1/bot-management/{botId}/material-categories` - Категории материалов
- `POST /api/v1/bot-management/{botId}/material-categories` - Создать категорию
- `GET /api/v1/bot-management/{botId}/materials` - Материалы бота
- `POST /api/v1/bot-management/{botId}/materials` - Создать материал
- `GET /api/v1/bot-management/{botId}/statistics` - Статистика бота
- `GET /api/v1/bot-management/{botId}/users` - Пользователи бота

Полный список API endpoints доступен в разделе документации админ-панели: `/admin/documentation`

## 🔐 Система ролей

Проект использует систему ролей для управления доступом:

- **admin** - Полный доступ ко всем разделам админ-панели (включая управление ботами, логи, управление ботом АИП)
- **manager** - Доступ к Медиа, Уведомлениям, Поддержке, Подписке, Документации
- **user** - Доступ только к Документации

Роли назначаются через админ-панель в разделе "Роли" (только для администраторов).

## 🔄 Автоматический деплой

### GitHub Actions

При каждом push в ветку `main` автоматически запускается деплой на сервер.

**Настройка:**
1. Добавьте Secrets в GitHub:
   - `DEPLOY_SERVER_URL` - URL вашего сервера
   - `DEPLOY_TOKEN` - Токен для авторизации деплоя

2. Токен `DEPLOY_TOKEN` должен быть одинаковым в:
   - GitHub Secrets
   - `.env` файле на сервере

### GitHub Webhook

Настройте webhook в настройках репозитория GitHub:
- URL: `https://your-domain.com/api/webhook/github`
- Content type: `application/json`
- Secret (опционально): `GITHUB_WEBHOOK_SECRET`

### Процесс деплоя

При срабатывании деплоя выполняется:
1. Обновление кода из репозитория (`git pull`)
2. Установка зависимостей (`composer install`)
3. Выполнение миграций (`php artisan migrate`)
4. Очистка кешей
5. Оптимизация приложения

## 🔧 Конфигурация

### Переменные окружения

Основные переменные в `.env`:

```env
# Приложение
APP_NAME="Your App"
APP_ENV=production
APP_DEBUG=false
APP_URL=https://your-domain.com

# База данных
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=your_database
DB_USERNAME=your_username
DB_PASSWORD=your_password

# Telegram (опционально, если используете управление ботами через админ-панель)
TELEGRAM_BOT_TOKEN=your_bot_token

# Подписка (опционально)
SUBSCRIPTION_API_URL=https://crm.example.com/api/v1/subscription/check
SUBSCRIPTION_API_TOKEN=your_subscription_token

# Деплой (опционально)
DEPLOY_TOKEN=your_deploy_token
DEPLOY_SERVER_URL=https://your-domain.com

# GitHub Webhook (опционально)
GITHUB_WEBHOOK_SECRET=your_webhook_secret
```

## 📖 Документация

Полная техническая документация доступна в админ-панели по адресу `/admin/documentation`.

Документация включает:
- Обзор системы
- Описание всех API endpoints
- Руководство по админ-панели
- Управление ботами и настройками (v1.2.0+)
- Инструкции по деплою

**Дополнительная документация:**
- [VERSIONING.md](VERSIONING.md) - Версионирование проекта и установка конкретных версий
- [DEPLOY_DOCUMENTATION.md](DEPLOY_DOCUMENTATION.md) - Документация по деплою
- [SUPPORT_API_DOCUMENTATION.md](SUPPORT_API_DOCUMENTATION.md) - Документация API поддержки

## 🔒 Безопасность

- Валидация всех входных данных
- Защита API endpoints через middleware
- SSL/TLS обязателен для production
- Защита от CSRF атак
- Безопасное хранение паролей (bcrypt)
- Защита webhook'ов токенами

## 📝 Лицензия

Проект разработан для использования в коммерческих целях.

## 🤝 Поддержка

При возникновении проблем:
1. Проверьте логи в `storage/logs/laravel.log`
2. Убедитесь, что все зависимости установлены
3. Проверьте настройки в `.env`
4. Посетите раздел документации в админ-панели

## 🔄 Обновление

### Обновление до последней версии (main)

При обновлении проекта до последней версии из ветки main:

```bash
git pull origin main
composer install
npm install
php artisan migrate
php artisan optimize:clear
npm run build:admin
php artisan optimize
```

### Обновление до конкретной версии

Для обновления до определенной стабильной версии используйте git теги. См. [VERSIONING.md](VERSIONING.md) для подробной информации.

```bash
# Получить все теги
git fetch --tags

# Переключиться на нужную версию
git checkout v1.0.0

# Выполнить стандартные шаги обновления
composer install
npm install
php artisan migrate
php artisan optimize:clear
npm run build:admin
php artisan optimize
```

---

**Текущая версия:** v1.2.0  
**Последнее обновление:** 27 декабря 2025  
**Репозиторий:** https://github.com/letoceiling-coder/aip

### Что нового в v1.2.0

- ✅ Полнофункциональный бот для Аудиторско-консалтинговой группы «АИП»
- ✅ Система проверки подписки на обязательный Telegram-канал
- ✅ Управление заявками на консультации с фильтрацией и изменением статусов
- ✅ Категории материалов с поддержкой внешних ссылок (открываются в Mini App)
- ✅ Управление материалами для скачивания (загрузка, медиа-библиотека, URL, Telegram file_id)
- ✅ Полная настройка текстов сообщений бота через админ-панель
- ✅ Уведомления администраторам о новых заявках в Telegram
- ✅ Статистика работы бота (пользователи, заявки, скачивания)
- ✅ FSM для управления состояниями пользователей
- ✅ Динамическая валидация полей формы
- ✅ Просмотр логов системы в админ-панели
- ✅ Обновленная документация по управлению ботом

### Что нового в v1.1.0

- ✅ Система управления Telegram ботами через админ-панель
- ✅ Автоматическая регистрация webhook при создании бота
- ✅ Обработка команды /start с автоматической отправкой приветственного сообщения
- ✅ Настройка параметров webhook (allowed_updates, max_connections, secret_token)
- ✅ Подробное логирование всех операций с ботами

См. [VERSIONING.md](VERSIONING.md) для полной истории версий.
